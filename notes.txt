xavier: XML/A Visualizer

#1 mandatory dimensions
#2 support for "attribute hierarchies"
  - hierarchy with one proper level and optionally an all level
  - flatten the hierarchy,

#3 add option so query designed can be configured to only allow a single level for a particular hierarchy.
#4 excel export: export date should be proper excel date.
#5 property support.
#6 "show parents" support in pivot table.
#7 Currently we can only drag a sort item out of the axis. Should be possible to drag it from the sort section into the hierarchies section
#8 pie charts needs labels
#9